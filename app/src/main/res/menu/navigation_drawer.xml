<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/setting_item"
        android:title="@string/setting">
        <menu>
            <item
                android:id="@+id/personal_setting"
                android:icon="@drawable/ic_personal_setting"
                android:title="@string/personal_setting"
                android:checkable="false"
                app:showAsAction="ifRoom"/>
            <item
                android:id="@+id/model_setting"
                android:icon="@drawable/ic_model_setting"
                android:title="@string/model_setting"
                android:checkable="false"
                app:showAsAction="ifRoom"/>
        </menu>
    </item>
    <item
        android:id="@+id/system_item"
        android:title="">
        <menu>
            <item
                android:id="@+id/exit"
                android:icon="@drawable/ic_exit"
                android:title="@string/exit"
                android:checkable="true"
                app:showAsAction="ifRoom"/>
        </menu>
    </item>
</menu>